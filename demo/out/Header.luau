-- Auto-generated by rbx-tsx
-- Source: Header.tsx

local React = require(game:GetService("ReplicatedStorage").Packages.React)
-- 
type HeaderProps = {
    gems: number,
    power: number,
}
local function Header(props: HeaderProps)
    local gems = props.gems
    local power = props.power
    return React.createElement("Frame", { [React.Tag] = "header" }, {
        TitleH1 = React.createElement("TextLabel", { [React.Tag] = "title", Text = "Gem Clicker" }),
        ScoreSpan = React.createElement("TextLabel", { [React.Tag] = "score", Text = tostring(gems) .. " Gems" }),
        InfoSpan = React.createElement("TextLabel", { [React.Tag] = "info", Text = tostring(power) .. " per click" }),
    })
end
-- 
return Header
