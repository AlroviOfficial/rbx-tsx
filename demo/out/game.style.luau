-- Auto-generated by rbx-css
-- Source: C:/Users/alexr/Programming/RandomProjects/rbx-tsx/demo/src/game.css

local function createStyleSheet()
    local sheet = Instance.new("StyleSheet")
    sheet.Name = "StyleSheet"

    -- Tokens from :root
    sheet:SetAttribute("bg", Color3.fromHex("#1a1a2e"))
    sheet:SetAttribute("surface", Color3.fromHex("#16213e"))
    sheet:SetAttribute("surface-hover", Color3.fromHex("#1a2744"))
    sheet:SetAttribute("primary", Color3.fromHex("#e94560"))
    sheet:SetAttribute("primary-hover", Color3.fromHex("#ff6b81"))
    sheet:SetAttribute("gold", Color3.fromHex("#ffd700"))
    sheet:SetAttribute("text", Color3.fromHex("#eaeaea"))
    sheet:SetAttribute("text-dim", Color3.fromHex("#8888a0"))
    sheet:SetAttribute("radius", UDim.new(0, 8))
    sheet:SetAttribute("gap", UDim.new(0, 8))

    -- Rule: .game
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".game"
        rule.Parent = sheet
        rule:SetProperties({
            BackgroundColor3 = "$bg",
            Size = UDim2.new(1, 0, 1, 0),
        })
    end

    -- Rule: .game::UIPadding
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".game::UIPadding"
        rule.Parent = sheet
        rule:SetProperties({
            PaddingTop = UDim.new(0, 16),
            PaddingBottom = UDim.new(0, 16),
            PaddingLeft = UDim.new(0, 16),
            PaddingRight = UDim.new(0, 16),
        })
    end

    -- Rule: .game::UIListLayout
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".game::UIListLayout"
        rule.Parent = sheet
        rule:SetProperties({
            FillDirection = Enum.FillDirection.Vertical,
            Padding = UDim.new(0, 12),
        })
    end

    -- Rule: .header
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".header"
        rule.Parent = sheet
        rule:SetProperties({
            AutomaticSize = Enum.AutomaticSize.Y,
            Size = UDim2.new(1, 0, 0, 0),
        })
    end

    -- Rule: .header::UIPadding
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".header::UIPadding"
        rule.Parent = sheet
        rule:SetProperties({
            PaddingTop = UDim.new(0, 8),
            PaddingBottom = UDim.new(0, 8),
            PaddingLeft = UDim.new(0, 8),
            PaddingRight = UDim.new(0, 8),
        })
    end

    -- Rule: .header::UIListLayout
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".header::UIListLayout"
        rule.Parent = sheet
        rule:SetProperties({
            FillDirection = Enum.FillDirection.Vertical,
            HorizontalAlignment = Enum.HorizontalAlignment.Center,
            Padding = UDim.new(0, 4),
        })
    end

    -- Rule: .title
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".title"
        rule.Parent = sheet
        rule:SetProperties({
            TextColor3 = "$gold",
            TextSize = 36,
            FontFace = Font.new("rbxasset://fonts/families/GothamSSm.json", Enum.FontWeight.Bold),
        })
    end

    -- Rule: .score
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".score"
        rule.Parent = sheet
        rule:SetProperties({
            TextColor3 = "$text",
            TextSize = 28,
            FontFace = Font.new("rbxasset://fonts/families/GothamSSm.json", Enum.FontWeight.SemiBold),
        })
    end

    -- Rule: .info
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".info"
        rule.Parent = sheet
        rule:SetProperties({
            TextColor3 = "$text-dim",
            TextSize = 14,
            FontFace = Font.new("rbxasset://fonts/families/GothamSSm.json"),
        })
    end

    -- Rule: .click-area
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".click-area"
        rule.Parent = sheet
        rule:SetProperty("Size", UDim2.new(1, 0, 0, 0))
    end

    -- Rule: .click-area::UIListLayout
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".click-area::UIListLayout"
        rule.Parent = sheet
        rule:SetProperties({
            FillDirection = Enum.FillDirection.Horizontal,
            HorizontalAlignment = Enum.HorizontalAlignment.Center,
            VerticalAlignment = Enum.VerticalAlignment.Center,
        })
    end

    -- Rule: .click-area::UIFlexItem
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".click-area::UIFlexItem"
        rule.Parent = sheet
        rule:SetProperties({
            FlexMode = Enum.UIFlexMode.Custom,
            GrowRatio = 1,
        })
    end

    -- Rule: .gem-button
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".gem-button"
        rule.Parent = sheet
        rule:SetProperties({
            BackgroundColor3 = "$primary",
            TextSize = 24,
            TextColor3 = Color3.fromRGB(255, 255, 255),
            Size = UDim2.new(0, 180, 0, 180),
            FontFace = Font.new("rbxasset://fonts/families/GothamSSm.json", Enum.FontWeight.Bold),
        })
    end

    -- Rule: .gem-button::UICorner
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".gem-button::UICorner"
        rule.Parent = sheet
        rule:SetProperty("CornerRadius", UDim.new(0, 90))
    end

    -- Rule: .gem-button:Hover
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".gem-button:Hover"
        rule.Parent = sheet
        rule:SetProperty("BackgroundColor3", "$primary-hover")
    end

    -- Rule: .shop
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".shop"
        rule.Parent = sheet
        rule:SetProperties({
            AutomaticSize = Enum.AutomaticSize.Y,
            Size = UDim2.new(1, 0, 0, 0),
        })
    end

    -- Rule: .shop::UIPadding
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".shop::UIPadding"
        rule.Parent = sheet
        rule:SetProperties({
            PaddingTop = UDim.new(0, 8),
            PaddingBottom = UDim.new(0, 8),
            PaddingLeft = UDim.new(0, 8),
            PaddingRight = UDim.new(0, 8),
        })
    end

    -- Rule: .shop::UIListLayout
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".shop::UIListLayout"
        rule.Parent = sheet
        rule:SetProperties({
            FillDirection = Enum.FillDirection.Vertical,
            Padding = "$gap",
        })
    end

    -- Rule: .shop-header
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".shop-header"
        rule.Parent = sheet
        rule:SetProperties({
            TextColor3 = "$gold",
            TextSize = 22,
            FontFace = Font.new("rbxasset://fonts/families/GothamSSm.json", Enum.FontWeight.Bold),
        })
    end

    -- Rule: .shop-list
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".shop-list"
        rule.Parent = sheet
        rule:SetProperties({
            AutomaticSize = Enum.AutomaticSize.Y,
            Size = UDim2.new(1, 0, 0, 0),
        })
    end

    -- Rule: .shop-list::UIPadding
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".shop-list::UIPadding"
        rule.Parent = sheet
        rule:SetProperties({
            PaddingTop = UDim.new(0, 4),
            PaddingBottom = UDim.new(0, 4),
            PaddingLeft = UDim.new(0, 4),
            PaddingRight = UDim.new(0, 4),
        })
    end

    -- Rule: .shop-list::UIListLayout
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".shop-list::UIListLayout"
        rule.Parent = sheet
        rule:SetProperties({
            FillDirection = Enum.FillDirection.Vertical,
            Padding = "$gap",
        })
    end

    -- Rule: .shop-item
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".shop-item"
        rule.Parent = sheet
        rule:SetProperties({
            BackgroundColor3 = "$surface",
            AutomaticSize = Enum.AutomaticSize.Y,
            Size = UDim2.new(1, 0, 0, 0),
        })
    end

    -- Rule: .shop-item::UIPadding
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".shop-item::UIPadding"
        rule.Parent = sheet
        rule:SetProperties({
            PaddingTop = UDim.new(0, 12),
            PaddingBottom = UDim.new(0, 12),
            PaddingLeft = UDim.new(0, 12),
            PaddingRight = UDim.new(0, 12),
        })
    end

    -- Rule: .shop-item::UICorner
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".shop-item::UICorner"
        rule.Parent = sheet
        rule:SetProperty("CornerRadius", "$radius")
    end

    -- Rule: .shop-item::UIListLayout
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".shop-item::UIListLayout"
        rule.Parent = sheet
        rule:SetProperties({
            FillDirection = Enum.FillDirection.Horizontal,
            VerticalAlignment = Enum.VerticalAlignment.Center,
            Padding = UDim.new(0, 12),
        })
    end

    -- Rule: .shop-item:Hover
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".shop-item:Hover"
        rule.Parent = sheet
        rule:SetProperty("BackgroundColor3", "$surface-hover")
    end

    -- Rule: .item-info
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".item-info"
        rule.Parent = sheet
        rule:SetProperty("AutomaticSize", Enum.AutomaticSize.XY)
    end

    -- Rule: .item-info::UIListLayout
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".item-info::UIListLayout"
        rule.Parent = sheet
        rule:SetProperties({
            FillDirection = Enum.FillDirection.Vertical,
            Padding = UDim.new(0, 2),
        })
    end

    -- Rule: .item-info::UIFlexItem
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".item-info::UIFlexItem"
        rule.Parent = sheet
        rule:SetProperties({
            FlexMode = Enum.UIFlexMode.Custom,
            GrowRatio = 1,
        })
    end

    -- Rule: .item-name
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".item-name"
        rule.Parent = sheet
        rule:SetProperties({
            TextColor3 = "$text",
            TextSize = 16,
            FontFace = Font.new("rbxasset://fonts/families/GothamSSm.json", Enum.FontWeight.SemiBold),
        })
    end

    -- Rule: .item-desc
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".item-desc"
        rule.Parent = sheet
        rule:SetProperties({
            TextColor3 = "$text-dim",
            TextSize = 12,
            FontFace = Font.new("rbxasset://fonts/families/GothamSSm.json"),
        })
    end

    -- Rule: .item-cost
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".item-cost"
        rule.Parent = sheet
        rule:SetProperties({
            TextColor3 = "$gold",
            TextSize = 14,
            FontFace = Font.new("rbxasset://fonts/families/GothamSSm.json", Enum.FontWeight.SemiBold),
        })
    end

    -- Rule: .buy-btn
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".buy-btn"
        rule.Parent = sheet
        rule:SetProperties({
            BackgroundColor3 = "$primary",
            TextColor3 = Color3.fromRGB(255, 255, 255),
            TextSize = 14,
            AutomaticSize = Enum.AutomaticSize.XY,
            FontFace = Font.new("rbxasset://fonts/families/GothamSSm.json", Enum.FontWeight.SemiBold),
        })
    end

    -- Rule: .buy-btn::UIPadding
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".buy-btn::UIPadding"
        rule.Parent = sheet
        rule:SetProperties({
            PaddingTop = UDim.new(0, 8),
            PaddingBottom = UDim.new(0, 8),
            PaddingLeft = UDim.new(0, 20),
            PaddingRight = UDim.new(0, 20),
        })
    end

    -- Rule: .buy-btn::UICorner
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".buy-btn::UICorner"
        rule.Parent = sheet
        rule:SetProperty("CornerRadius", UDim.new(0, 6))
    end

    -- Rule: .buy-btn:Hover
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".buy-btn:Hover"
        rule.Parent = sheet
        rule:SetProperty("BackgroundColor3", "$primary-hover")
    end

    -- Rule: .buy-btn-locked
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".buy-btn-locked"
        rule.Parent = sheet
        rule:SetProperties({
            BackgroundColor3 = Color3.fromRGB(42, 42, 62),
            TextColor3 = Color3.fromRGB(85, 85, 112),
            TextSize = 14,
            AutomaticSize = Enum.AutomaticSize.XY,
            FontFace = Font.new("rbxasset://fonts/families/GothamSSm.json", Enum.FontWeight.SemiBold),
        })
    end

    -- Rule: .buy-btn-locked::UIPadding
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".buy-btn-locked::UIPadding"
        rule.Parent = sheet
        rule:SetProperties({
            PaddingTop = UDim.new(0, 8),
            PaddingBottom = UDim.new(0, 8),
            PaddingLeft = UDim.new(0, 20),
            PaddingRight = UDim.new(0, 20),
        })
    end

    -- Rule: .buy-btn-locked::UICorner
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".buy-btn-locked::UICorner"
        rule.Parent = sheet
        rule:SetProperty("CornerRadius", UDim.new(0, 6))
    end

    return sheet
end

return createStyleSheet
