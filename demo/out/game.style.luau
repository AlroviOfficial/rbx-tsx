-- Auto-generated by rbx-css
-- Source: C:\Users\alexr\Programming\RandomProjects\rbx-tsx\demo\src/game.css

local function createStyleSheet()
    local sheet = Instance.new("StyleSheet")
    sheet.Name = "StyleSheet"

    -- Tokens from :root
    sheet:SetAttribute("font-sans", "ui-sans-serif")
    sheet:SetAttribute("font-mono", "ui-monospace")
    sheet:SetAttribute("color-white", Color3.fromHex("#ffffff"))
    sheet:SetAttribute("color-bg", Color3.fromHex("#1a1a2e"))
    sheet:SetAttribute("color-surface", Color3.fromHex("#16213e"))
    sheet:SetAttribute("color-surface-hover", Color3.fromHex("#1a2744"))
    sheet:SetAttribute("color-primary", Color3.fromHex("#e94560"))
    sheet:SetAttribute("color-primary-hover", Color3.fromHex("#ff6b81"))
    sheet:SetAttribute("color-gold", Color3.fromHex("#ffd700"))
    sheet:SetAttribute("color-text", Color3.fromHex("#eaeaea"))
    sheet:SetAttribute("color-text-dim", Color3.fromHex("#8888a0"))
    sheet:SetAttribute("color-locked-bg", Color3.fromHex("#2a2a3e"))
    sheet:SetAttribute("color-locked-text", Color3.fromHex("#555570"))

    -- Rule: html
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = "html"
        rule.Parent = sheet
        rule:SetProperties({
            LineHeight = 1.5,
            FontFace = "$default-font-family",
        })
    end

    -- Rule: hr
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = "hr"
        rule.Parent = sheet
        rule:SetProperty("Size", UDim2.new(0, 0, 0, 0))
    end

    -- Rule: code
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = "code"
        rule.Parent = sheet
        rule:SetProperties({
            FontFace = "$default-mono-font-family",
            TextSize = 16,
        })
    end

    -- Rule: kbd
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = "kbd"
        rule.Parent = sheet
        rule:SetProperties({
            FontFace = "$default-mono-font-family",
            TextSize = 16,
        })
    end

    -- Rule: samp
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = "samp"
        rule.Parent = sheet
        rule:SetProperties({
            FontFace = "$default-mono-font-family",
            TextSize = 16,
        })
    end

    -- Rule: pre
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = "pre"
        rule.Parent = sheet
        rule:SetProperties({
            FontFace = "$default-mono-font-family",
            TextSize = 16,
        })
    end

    -- Rule: sub
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = "sub"
        rule.Parent = sheet
        rule:SetProperties({
            LineHeight = 0,
            TextYAlignment = Enum.TextYAlignment.Top,
        })
    end

    -- Rule: sup
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = "sup"
        rule.Parent = sheet
        rule:SetProperties({
            LineHeight = 0,
            TextYAlignment = Enum.TextYAlignment.Top,
        })
    end

    -- Rule: sup
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = "sup"
        rule.Parent = sheet
        rule:SetProperty("Position", UDim2.new(0, 0, 0, -8))
    end

    -- Rule: progress
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = "progress"
        rule.Parent = sheet
        rule:SetProperty("TextYAlignment", Enum.TextYAlignment.Top)
    end

    -- Rule: ImageLabel
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = "ImageLabel"
        rule.Parent = sheet
        rule:SetProperty("TextYAlignment", Enum.TextYAlignment.Center)
    end

    -- Rule: svg
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = "svg"
        rule.Parent = sheet
        rule:SetProperty("TextYAlignment", Enum.TextYAlignment.Center)
    end

    -- Rule: video
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = "video"
        rule.Parent = sheet
        rule:SetProperty("TextYAlignment", Enum.TextYAlignment.Center)
    end

    -- Rule: ViewportFrame
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = "ViewportFrame"
        rule.Parent = sheet
        rule:SetProperty("TextYAlignment", Enum.TextYAlignment.Center)
    end

    -- Rule: audio
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = "audio"
        rule.Parent = sheet
        rule:SetProperty("TextYAlignment", Enum.TextYAlignment.Center)
    end

    -- Rule: iframe
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = "iframe"
        rule.Parent = sheet
        rule:SetProperty("TextYAlignment", Enum.TextYAlignment.Center)
    end

    -- Rule: embed
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = "embed"
        rule.Parent = sheet
        rule:SetProperty("TextYAlignment", Enum.TextYAlignment.Center)
    end

    -- Rule: object
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = "object"
        rule.Parent = sheet
        rule:SetProperty("TextYAlignment", Enum.TextYAlignment.Center)
    end

    -- Rule: ImageLabel
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = "ImageLabel"
        rule.Parent = sheet
        rule:SetProperty("AutomaticSize", Enum.AutomaticSize.Y)
    end

    -- Rule: video
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = "video"
        rule.Parent = sheet
        rule:SetProperty("AutomaticSize", Enum.AutomaticSize.Y)
    end

    -- Rule: TextButton
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = "TextButton"
        rule.Parent = sheet
        rule:SetProperties({
            BackgroundColor3 = Color3.fromRGB(0, 0, 0),
            BackgroundTransparency = 0,
        })
    end

    -- Rule: TextButton::UICorner
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = "TextButton::UICorner"
        rule.Parent = sheet
        rule:SetProperty("CornerRadius", UDim.new(0, 0))
    end

    -- Rule: TextBox
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = "TextBox"
        rule.Parent = sheet
        rule:SetProperties({
            BackgroundColor3 = Color3.fromRGB(0, 0, 0),
            BackgroundTransparency = 0,
        })
    end

    -- Rule: TextBox::UICorner
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = "TextBox::UICorner"
        rule.Parent = sheet
        rule:SetProperty("CornerRadius", UDim.new(0, 0))
    end

    -- Rule: select
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = "select"
        rule.Parent = sheet
        rule:SetProperties({
            BackgroundColor3 = Color3.fromRGB(0, 0, 0),
            BackgroundTransparency = 0,
        })
    end

    -- Rule: select::UICorner
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = "select::UICorner"
        rule.Parent = sheet
        rule:SetProperty("CornerRadius", UDim.new(0, 0))
    end

    -- Rule: optgroup
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = "optgroup"
        rule.Parent = sheet
        rule:SetProperties({
            BackgroundColor3 = Color3.fromRGB(0, 0, 0),
            BackgroundTransparency = 0,
        })
    end

    -- Rule: optgroup::UICorner
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = "optgroup::UICorner"
        rule.Parent = sheet
        rule:SetProperty("CornerRadius", UDim.new(0, 0))
    end

    -- Rule: textarea
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = "textarea"
        rule.Parent = sheet
        rule:SetProperties({
            BackgroundColor3 = Color3.fromRGB(0, 0, 0),
            BackgroundTransparency = 0,
        })
    end

    -- Rule: textarea::UICorner
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = "textarea::UICorner"
        rule.Parent = sheet
        rule:SetProperty("CornerRadius", UDim.new(0, 0))
    end

    -- Rule:  optgroup option::UIPadding
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = " optgroup option::UIPadding"
        rule.Parent = sheet
        rule:SetProperty("PaddingLeft", UDim.new(0, 20))
    end

    -- Rule: .container
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".container"
        rule.Parent = sheet
        rule:SetProperty("Size", UDim2.new(1, 0, 0, 0))
    end

    -- Rule: .flex::UIListLayout
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".flex::UIListLayout"
        rule.Parent = sheet
        rule:SetProperty("FillDirection", Enum.FillDirection.Horizontal)
    end

    -- Rule: .h-auto
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".h-auto"
        rule.Parent = sheet
        rule:SetProperty("AutomaticSize", Enum.AutomaticSize.Y)
    end

    -- Rule: .h-full
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".h-full"
        rule.Parent = sheet
        rule:SetProperty("Size", UDim2.new(0, 0, 1, 0))
    end

    -- Rule: .w-auto
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".w-auto"
        rule.Parent = sheet
        rule:SetProperty("AutomaticSize", Enum.AutomaticSize.X)
    end

    -- Rule: .w-full
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".w-full"
        rule.Parent = sheet
        rule:SetProperty("Size", UDim2.new(1, 0, 0, 0))
    end

    -- Rule: .grow::UIFlexItem
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".grow::UIFlexItem"
        rule.Parent = sheet
        rule:SetProperties({
            FlexMode = Enum.UIFlexMode.Custom,
            GrowRatio = 1,
        })
    end

    -- Rule: .flex-col::UIListLayout
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".flex-col::UIListLayout"
        rule.Parent = sheet
        rule:SetProperty("FillDirection", Enum.FillDirection.Vertical)
    end

    -- Rule: .flex-row::UIListLayout
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".flex-row::UIListLayout"
        rule.Parent = sheet
        rule:SetProperty("FillDirection", Enum.FillDirection.Horizontal)
    end

    -- Rule: .items-center::UIListLayout
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".items-center::UIListLayout"
        rule.Parent = sheet
        rule:SetProperty("VerticalAlignment", Enum.VerticalAlignment.Center)
    end

    -- Rule: .justify-center::UIListLayout
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".justify-center::UIListLayout"
        rule.Parent = sheet
        rule:SetProperty("HorizontalAlignment", Enum.HorizontalAlignment.Center)
    end

    -- Rule: .gap-0.5::UIListLayout
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".gap-0.5::UIListLayout"
        rule.Parent = sheet
        rule:SetProperty("Padding", UDim.new(0, 2))
    end

    -- Rule: .gap-1::UIListLayout
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".gap-1::UIListLayout"
        rule.Parent = sheet
        rule:SetProperty("Padding", UDim.new(0, 4))
    end

    -- Rule: .gap-2::UIListLayout
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".gap-2::UIListLayout"
        rule.Parent = sheet
        rule:SetProperty("Padding", UDim.new(0, 8))
    end

    -- Rule: .gap-3::UIListLayout
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".gap-3::UIListLayout"
        rule.Parent = sheet
        rule:SetProperty("Padding", UDim.new(0, 12))
    end

    -- Rule: .rounded-lg::UICorner
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".rounded-lg::UICorner"
        rule.Parent = sheet
        rule:SetProperty("CornerRadius", UDim.new(0, 8))
    end

    -- Rule: .rounded-md::UICorner
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".rounded-md::UICorner"
        rule.Parent = sheet
        rule:SetProperty("CornerRadius", UDim.new(0, 6))
    end

    -- Rule: .bg-bg
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".bg-bg"
        rule.Parent = sheet
        rule:SetProperty("BackgroundColor3", "$color-bg")
    end

    -- Rule: .bg-locked-bg
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".bg-locked-bg"
        rule.Parent = sheet
        rule:SetProperty("BackgroundColor3", "$color-locked-bg")
    end

    -- Rule: .bg-primary
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".bg-primary"
        rule.Parent = sheet
        rule:SetProperty("BackgroundColor3", "$color-primary")
    end

    -- Rule: .bg-surface
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".bg-surface"
        rule.Parent = sheet
        rule:SetProperty("BackgroundColor3", "$color-surface")
    end

    -- Rule: .p-1::UIPadding
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".p-1::UIPadding"
        rule.Parent = sheet
        rule:SetProperties({
            PaddingTop = UDim.new(0, 4),
            PaddingBottom = UDim.new(0, 4),
            PaddingLeft = UDim.new(0, 4),
            PaddingRight = UDim.new(0, 4),
        })
    end

    -- Rule: .p-2::UIPadding
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".p-2::UIPadding"
        rule.Parent = sheet
        rule:SetProperties({
            PaddingTop = UDim.new(0, 8),
            PaddingBottom = UDim.new(0, 8),
            PaddingLeft = UDim.new(0, 8),
            PaddingRight = UDim.new(0, 8),
        })
    end

    -- Rule: .p-3::UIPadding
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".p-3::UIPadding"
        rule.Parent = sheet
        rule:SetProperties({
            PaddingTop = UDim.new(0, 12),
            PaddingBottom = UDim.new(0, 12),
            PaddingLeft = UDim.new(0, 12),
            PaddingRight = UDim.new(0, 12),
        })
    end

    -- Rule: .p-4::UIPadding
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".p-4::UIPadding"
        rule.Parent = sheet
        rule:SetProperties({
            PaddingTop = UDim.new(0, 16),
            PaddingBottom = UDim.new(0, 16),
            PaddingLeft = UDim.new(0, 16),
            PaddingRight = UDim.new(0, 16),
        })
    end

    -- Rule: .px-5::UIPadding
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".px-5::UIPadding"
        rule.Parent = sheet
        rule:SetProperties({
            PaddingLeft = UDim.new(0, 20),
            PaddingRight = UDim.new(0, 20),
        })
    end

    -- Rule: .py-2::UIPadding
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".py-2::UIPadding"
        rule.Parent = sheet
        rule:SetProperties({
            PaddingTop = UDim.new(0, 8),
            PaddingBottom = UDim.new(0, 8),
        })
    end

    -- Rule: .text-4xl
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".text-4xl"
        rule.Parent = sheet
        rule:SetProperty("TextSize", 36)
    end

    -- Rule: .text-base
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".text-base"
        rule.Parent = sheet
        rule:SetProperty("TextSize", 16)
    end

    -- Rule: .text-sm
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".text-sm"
        rule.Parent = sheet
        rule:SetProperty("TextSize", 14)
    end

    -- Rule: .text-xs
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".text-xs"
        rule.Parent = sheet
        rule:SetProperty("TextSize", 12)
    end

    -- Rule: .text-[22px]
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".text-[22px]"
        rule.Parent = sheet
        rule:SetProperty("TextSize", 22)
    end

    -- Rule: .text-[28px]
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".text-[28px]"
        rule.Parent = sheet
        rule:SetProperty("TextSize", 28)
    end

    -- Rule: .text-gold
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".text-gold"
        rule.Parent = sheet
        rule:SetProperty("TextColor3", "$color-gold")
    end

    -- Rule: .text-locked-text
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".text-locked-text"
        rule.Parent = sheet
        rule:SetProperty("TextColor3", "$color-locked-text")
    end

    -- Rule: .text-text
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".text-text"
        rule.Parent = sheet
        rule:SetProperty("TextColor3", "$color-text")
    end

    -- Rule: .text-text-dim
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".text-text-dim"
        rule.Parent = sheet
        rule:SetProperty("TextColor3", "$color-text-dim")
    end

    -- Rule: .text-white
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".text-white"
        rule.Parent = sheet
        rule:SetProperty("TextColor3", "$color-white")
    end

    -- Rule: .font-title
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".font-title"
        rule.Parent = sheet
        rule:SetProperty("FontFace", Font.new("rbxasset://fonts/families/GothamSSm.json", Enum.FontWeight.Bold))
    end

    -- Rule: .font-heading
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".font-heading"
        rule.Parent = sheet
        rule:SetProperty("FontFace", Font.new("rbxasset://fonts/families/GothamSSm.json", Enum.FontWeight.SemiBold))
    end

    -- Rule: .font-body
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".font-body"
        rule.Parent = sheet
        rule:SetProperty("FontFace", Font.new("rbxasset://fonts/families/GothamSSm.json"))
    end

    -- Rule: .gem-button
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".gem-button"
        rule.Parent = sheet
        rule:SetProperties({
            BackgroundColor3 = "$color-primary",
            TextSize = 24,
            TextColor3 = Color3.fromRGB(255, 255, 255),
            Size = UDim2.new(0, 180, 0, 180),
            FontFace = Font.new("rbxasset://fonts/families/GothamSSm.json", Enum.FontWeight.Bold),
        })
    end

    -- Rule: .gem-button::UICorner
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".gem-button::UICorner"
        rule.Parent = sheet
        rule:SetProperty("CornerRadius", UDim.new(0, 90))
    end

    -- Rule: .gem-button:Hover
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".gem-button:Hover"
        rule.Parent = sheet
        rule:SetProperty("BackgroundColor3", "$color-primary-hover")
    end

    -- Rule: .hover:bg-primary-hover:Hover
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".hover:bg-primary-hover:Hover"
        rule.Parent = sheet
        rule:SetProperty("BackgroundColor3", "$color-primary-hover")
    end

    -- Rule: .hover:bg-surface-hover:Hover
    do
        local rule = Instance.new("StyleRule")
        rule.Selector = ".hover:bg-surface-hover:Hover"
        rule.Parent = sheet
        rule:SetProperty("BackgroundColor3", "$color-surface-hover")
    end

    return sheet
end

return createStyleSheet
